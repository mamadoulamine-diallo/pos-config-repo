server:
  port: 8084

spring:
  application:
    name: activity-service

  mongodb:
    uri: mongodb://localhost:27017/pos_activity_db

  cloud:
    consul:
      host: localhost
      port: 8500

management:
  endpoints:
    web:
      exposure:
        include: health,info