server:
  port: 8084

spring:
  application:
    name: activity-service

  cloud:
    consul:
      host: localhost
      port: 8500

management:
  endpoints:
    web:
      exposure:
        include: health,info