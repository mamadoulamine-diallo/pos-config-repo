server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/pos_user_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: Chosenprince

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always